# CNIC OCR microservice — Python 3.11

fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
python-multipart==0.0.20

# OCR — RapidOCR on ONNX Runtime (CPU-only). Replaces PaddleOCR / PaddlePaddle,
# whose native runtime SIGSEGV'd on the container. Same PP-OCRv4 models, shipped
# inside the rapidocr-onnxruntime wheel (no runtime model download).
rapidocr-onnxruntime==1.4.4
onnxruntime==1.19.2
opencv-python==4.10.0.84
numpy==1.26.4
